{
  "name": "next-express-monorepo",
  "private": true,
  "version": "1.0.0",
  "packageManager": "pnpm@10.29.3",
  "scripts": {
    "dev": "concurrently -n client,server,admin -c cyan,green,yellow \"pnpm --filter @repo/client dev\" \"pnpm --filter @repo/server dev\" \"pnpm --filter @repo/admin dev\"",
    "dev:client": "pnpm --filter @repo/client dev",
    "dev:server": "pnpm --filter @repo/server dev",
    "build": "pnpm --filter @repo/client build && pnpm --filter @repo/server build",
    "typecheck": "pnpm --filter @repo/client typecheck && pnpm --filter @repo/server typecheck",
    "start": "concurrently -n client,server -c cyan,green \"pnpm --filter @repo/client start\" \"pnpm --filter @repo/server start\"",
    "start:client": "pnpm --filter @repo/client start",
    "start:server": "pnpm --filter @repo/server start",
    "db:up": "docker compose --env-file .env.postgres up -d postgres",
    "db:down": "docker compose --env-file .env.postgres down",
    "db:generate": "pnpm --filter @repo/server db:generate",
    "db:migrate": "pnpm --filter @repo/server db:migrate",
    "db:push": "pnpm --filter @repo/server db:push",
    "db:check": "pnpm --filter @repo/server db:check",
    "db:studio": "pnpm --filter @repo/server db:studio",
    "test:sentry": "pnpm --filter @repo/server test:sentry",
    "test": "pnpm --filter @repo/server test && pnpm --filter @repo/client test",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "prepare": "husky",
    "heroku-postbuild": "cd node_modules/.pnpm/bcrypt@6.0.0/node_modules/bcrypt && npm run install && cd /app && pnpm --filter @repo/server build"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "bcrypt"
    ]
  },
  "devDependencies": {
    "@playwright/test": "^1.60.0",
    "@tailwindcss/postcss": "^4",
    "concurrently": "^9.2.0",
    "husky": "^9.1.7",
    "lint-staged": "^17.0.4",
    "prettier": "^3.8.3",
    "tailwindcss": "^4"
  }
}
